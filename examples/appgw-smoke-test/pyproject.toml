[project]
name = "appgw-smoke-test-runner"
version = "0.1.0"
description = "Runs the App Gateway v2 TCP/TLS smoke test against Databricks Serverless via Databricks Connect"
requires-python = ">=3.12,<3.13"
dependencies = [
    "databricks-connect>=18.1,<18.2",
    "databricks-sdk>=0.40",
]
